- **렌더링 3단계**: 브라우저는 화면을 그릴 때 Layout(위치/크기 계산) -> Paint(색칠) -> Composite(레이어 합성) 과정을 거침.
- **성능 저하 원인**: `top`, `left`, `width` 등의 속성을 애니메이션하면 가장 무거운 연산인 Layout(Reflow)이 반복해서 발생함.
- **해결책 (GPU 가속)**: 애니메이션 시 `transform`과 `opacity`를 사용하면 브라우저가 연산을 GPU에 위임하여(하드웨어 가속) 버벅임 없이 60fps의 부드러운 렌더링 보장.
