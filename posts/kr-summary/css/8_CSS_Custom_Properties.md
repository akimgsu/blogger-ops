- **CSS 변수 선언/사용**: `:root`에 `--변수명` 형태로 선언하고, `var(--변수명)`으로 사용하여 코드 중복 방지 및 유지보수성 향상.
- **다크 모드 최적화**: 모든 컴포넌트의 색상을 개별 수정할 필요 없이, 테마 상태에 따라 `:root`의 변수 값만 갈아끼우는 방식으로 설계.
- **구현 방식**: 시스템 설정은 `@media (prefers-color-scheme: dark)`로 감지하며, 수동 전환은 자바스크립트를 이용해 최상위 요소에 `data-theme` 속성을 토글하여 제어.
