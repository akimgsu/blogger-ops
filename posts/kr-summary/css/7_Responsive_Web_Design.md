- **반응형 웹(RWD)**: 하나의 코드로 모바일, 태블릿, 데스크톱 화면 크기에 맞게 최적화된 레이아웃을 제공하는 기술.
- **미디어 쿼리(`@media`)**: 화면 너비(`min-width`, `max-width`)를 기준으로 조건부 CSS를 적용하여 기기별 분기 처리.
- **가변 단위 활용**: `px` 대신 최상위 폰트 크기 기준인 `rem`, 뷰포트 비율인 `vw/vh`를 사용하여 유연하고 비율이 맞는 UI 구축.
