- **CSS Grid의 목적**: Flexbox와 달리 행(Row)과 열(Column)을 동시에 다루는 2차원 레이아웃 시스템.
- **핵심 단위 `fr`**: 부모의 남은 공간을 비율로 나누어 차지하는 유연한 단위.
- **`grid-template-areas`**: 문자열을 통해 각 요소가 차지할 영역을 시각적으로 매핑하여, 복잡한 레이아웃과 반응형 디자인을 매우 직관적으로 처리함.
