- **Coercion**: 연산 맥락에 맞춰 암묵적 타입 변환이 일어남.
- **Falsy**: `0`, `""`, `null`, `undefined`, `NaN` 등 — `[]`/`{}`는 truthy.
- **비교**: 기본은 `===`, nullish만 `== null`을 허용하는 팀이 많음.
- **경계**: `+`와 `Number`/`parseInt` 차이를 명시적으로 다룸.
- **방어**: 입력 검증, `??`/`?.`, 런타임 스키마, 린트/타입이 한 세트임.
