- **복사 vs 공유**: Primitive는 값 복사, Object는 참조 공유임.
- **인자 전달**: 값으로 전달되지만 객체의 값은 참조라 내부 변이가 외부에 보임.
- **불변성**: 기존 데이터를 바꾸지 않고 새 스냅샷을 만드는 실천임.
- **얕은 복사**: 스프레드는 shallow — 중첩은 경로별 복사 또는 `structuredClone`.
- **freeze**: `Object.freeze`도 shallow이며 설계 패턴이 더 중요함.
