- **파이프라인**: V8는 Parser → AST → Ignition(bytecode) → TurboFan(JIT) → (필요 시) Deopt로 실행함.
- **Hot code**: 처음부터 전부 네이티브 컴파일하지 않고, 자주 도는 코드만 깊게 최적화함.
- **AST**: 린트/트랜스파일/번들러의 공통 중간 표현임.
- **Deopt**: 타입·객체 shape 가정이 깨지면 최적화 코드가 폐기됨.
- **실무**: 일관된 데이터 shape와 좁은 타입이 V8 친화적임.
